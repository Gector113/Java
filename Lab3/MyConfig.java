package org.fpm.di.config;

import org.fpm.di.binder.Binder;

public class MyConfig implements Configuration {

    @Override
    public void configure(Binder binder) {
        //binder.bind
    }
}
